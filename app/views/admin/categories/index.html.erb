<div class="container">
  <div class="h3 p-3"><%= "カテゴリー「#{@category.name}」" %></div>
  <div class="row">
    <%= render 'admin/shared/article_index', {articles: @articles, admin: @admin} %>
  </div>
  <%= paginate @articles %>
</div>